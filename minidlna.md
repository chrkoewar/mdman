# minidlna
```
sudo service minidlna restart
autostart: update-rc.d minidlna defaults
!! https://bbrks.me/rpi-minidlna-media-server/
/etc/minidlna.conf
sudo service minidlna start
minidlna -r         rescan media files
autostart: update-rc.d minidlna defaults
```

